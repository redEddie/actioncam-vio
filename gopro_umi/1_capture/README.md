# Capture source ownership

The actioncam-vio capture/VIO source is the root of this Git repository. It is
not duplicated recursively inside this canonical `gopro_umi` source snapshot.

In the local canonical workspace the same component lives at
`1_capture/actioncam-vio/`. Raw `Episode/` videos and generated
`Episode_result/` content are intentionally excluded from Git.
